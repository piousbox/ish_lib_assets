require 'spec_helper'

describe IshLibAssets do
  it 'has a version number' do
    expect(IshLibAssets::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
